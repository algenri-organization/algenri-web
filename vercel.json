{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ]; then BASE=HEAD^; else git fetch origin main --depth=1 >/dev/null 2>&1 || true; BASE=origin/main; fi; test -z \"$(git diff --name-only $BASE...HEAD 2>/dev/null | grep -vE '^(docs/|.*\\.md$)')\""
}
